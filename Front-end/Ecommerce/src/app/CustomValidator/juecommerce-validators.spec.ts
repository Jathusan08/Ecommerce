import { JUEcommerceValidators } from './juecommerce-validators';

describe('JUEcommerceValidators', () => {
  it('should create an instance', () => {
    expect(new JUEcommerceValidators()).toBeTruthy();
  });
});
